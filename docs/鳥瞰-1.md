# 第一輪鳥瞰(2026-07-13;七條規格齊備、T6/T7 Lean 未動時)

逐條裁決帳本「鳥瞰議程」;結論先行:**藍圖無需結構性變更**;新增兩個論文級
「詞典」小節、確認一條論文後研究支線、棄一條、緩一條、議程加一條新線索。

## 1. codim-1 現象的定位 → **確認為論文後主支線**

T6(任意維唯一性)+ T7 F1(計數表示 ⟺ codim 1)已把機制邊界定理化。
剩餘的真問題:**高維障礙理論**——`dim span(f) ≥ 4` 時 `[c_f]` 的取值行為
(對話紀錄已載 `⟨η, c_f⟩ = 1` 的反例存在),分類「flow 的幾何 → 障礙類」。
這超出本論文範圍,是天然的下一篇;本文以「branch」一節指出即可(已在藍圖)。

## 2. 細胞層/上同調讀法 → **採納為論文詞典小節(不改證明、不進 Lean)**

精確對應:`δ_f` 是圖上細胞層(頂點 stalk `Γ`、邊 stalk `Q_e`、限制映射 = 商)
的 Čech 微分;`coker δ_f = H¹(G; 𝓕)`;T1 使局部解集成為 `Γ`-torsor 層,
黏合資料的類 `[c_f] ∈ H¹` 恰是 torsor 平凡化的標準障礙。**價值**:定位進
cellular sheaf 文獻(也回答「有沒有人做過」——框架有人做,這個層沒有)、
給內行讀者一行看懂的總綱。**不改證明**:H¹ 形式化只會增重,Lean 不採。

## 3. dart 對偶 / Fano 幾何 → **採納為論文詞典小節**

`Γ = F₂³` 時:非零向量 = Fano 平面 PG(2,2) 的 7 點;平面 `W` = Fano 線
(其 3 點 = 3 個方向)。故 NWZ 流 = 「頂點 ↦ Fano 線、dart ↦ 線上的點,
沿邊共點」的關聯幾何標號;`κ_{v,e}` = 線 `W_v` 上除 `f(e)` 外的殘對;
「Fano flow」之名由此落地。**價值**:F1 的展示可在一條 Fano 線上完成;
歷史敘事(BJJ 的 Fano 框架)獲得幾何載體。不改定理內容。

## 4. |L(W)| = |Γ| 的 species/groupoid 讀法 → **棄**

torsor 陳述已是全部內容;無新增定理跡象。

## 5. 4-cover → 2-cover 壓縮 → **緩(寫史料節時做)**

猜測:BJJ 的 circuit 4-cover 相當於「不解相容方程、兩端各自取集」的
未黏合版本(每邊 4 元素);我們的 `[c_f] = 0` 恰是把 4 壓成 2 的條件。
若坐實,related-work 一節從「引用」升格為「機制對比」。寫作期驗證。

## 6. 上游化候選審計 → 收官後(已有決策)

## 7.(新增)E3′ 的表示論定位 → **議程存查**

「對 unipotent(剪切)子群的不變泛函唯一」形似 F₂ 上拋物子群/Steinberg
式的標準事實;寫作期查文獻,若有現成引理則 E3′ 的證明可加一句定位
(不影響自足證明)。

## 對藍圖的淨效果

- 論文新增兩個小節:§詞典(層讀法)、§詞典(Fano 關聯幾何)——皆為表述層。
- 定理集 T1–T7 不變;Lean 範圍不變。
- 下一篇論文的種子註冊:高維障礙分類(支線 1)。
