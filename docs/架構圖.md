# 項目架構圖紙(真數學依賴)

不是工程進度表:節點 = 數學對象/定理,實線 = 證明中真實使用,虛線 = 詮釋/對照。
本項目的深層結構是**兩條線 + 一座橋**:

- **存在線**(左):從特徵二算術一路構造到 CDC——每個對象「能用」;
- **必然線**(右):T2/B2/C1/E3 逐一證明存在線上每個對象是**被迫的**
  ——分類、泛性、完整不變量、唯一位元。這條線全程與維數無關;
- **橋**(下):把原稿文本錨定為存在線的一次規範選擇。

`dim Γ = 3` 在全圖**恰有一個閘門**:F1(以 codim-1 形式,
Lean 中即 `annihilator_unique` 一條引理)。

```mermaid
flowchart TB
  subgraph 基礎["基礎(Basic / Rank)"]
    A0["char-2 算術"]
    A1["平面 IsPlane(Klein 三元組)<br/>⟺ finrank W = 2(Rank 橋,座標隔離)"]
    A2["線 = Γ/⟨h⟩ 的元素(LineSpace)"]
  end

  subgraph 存在線["存在線(構造 → CDC)"]
    T1["T1 局部分類<br/>Φ_W : Γ ≃ L(W);κ 無選擇"]
    B4["B4 標籤公式 [m]+κ"]
    F0["F0 dart 圖層<br/>σ 對合 + 頂點平面 + cubic 雙射"]
    C3["C3 黏合方程 δ_f m = c_f<br/>(cfam_eq_kappa)"]
    F1["F1 分支恆等式 β = 支集奇偶<br/>★ 唯一 dim-3 閘門(codim-1)★"]
    F2["F2 Fano 相容性定理<br/>∃m, δ_f m = c_f(庫終點)"]
    F3["F3 全局偶族(標籤黏合)"]
    G4["G4 CDC(三次+NWZ流)<br/>⬜ 計劃中"]
  end

  subgraph 必然線["必然線(每個對象被迫;全程任意 Γ)"]
    T2["T2 自然性 + torsor<br/>(L(W) 的表示定理)"]
    B2["B2 邊商泛性<br/>(忘基點 ⟹ Q_e 被迫)"]
    B3["B3 κ = W/⟨h⟩ 唯一非零"]
    C1["C1 規範分類<br/>[c] ∈ coker δ 完整不變量"]
    C2["C2 對偶判準"]
    D["T5 K(W) 與 β 良定(D1–D3)"]
    E3["E3 β 唯一性(剪切;任意 Γ)"]
  end

  subgraph CDC抽取["G:CDC 抽取(⬜ 下一里程碑)"]
    G1["G1 每頂點 0-或-2 覆蓋"]
    G2["G2 圈 = ρ-置換軌道<br/>(ρ = 頂點配對∘σ)"]
    G3["G3 每邊恰兩次(|P_e| = 2)"]
  end

  subgraph 橋["橋(考古錨定;論文「等價重構」主張)"]
    SLOT["內化引理 slotFamily = Φ(t + f(b))<br/>原稿式(2)(3) = 一次規範選擇"]
    DCF["[d] ~ c_f(論文一行,經 C1)"]
    NOTES["comparison-notes(源碼行號對照)"]
  end

  subgraph 論文層["論文專屬(不入 Lean)"]
    P1["詞典:細胞層/H¹ 讀法"]
    P2["詞典:Fano 關聯幾何"]
    P3["F1(2) codim≥2 反例<br/>(紙面已證;Lean 低優先)"]
    P4["支線:高維障礙分類(下一篇)"]
  end

  A0 --> T1
  A1 --> T1
  A2 --> T1
  T1 --> B4
  A2 --> B2
  T1 --> B3
  B3 --> B4
  A1 --> F0
  B4 --> C3
  F0 --> C3
  D --> F1
  F1 --> F2
  C2 --> F2
  C3 --> F2
  D --> F2
  F2 --> F3
  B4 --> F3
  F3 --> G1
  T1 --> G1
  F0 --> G2
  A2 --> G3
  G1 --> G4
  G2 --> G4
  G3 --> G4

  T1 --> T2
  A1 --> D
  B3 --> E3
  D --> E3

  T1 --> SLOT
  SLOT --> DCF
  C1 --> DCF
  SLOT -.-> NOTES

  T2 -.-> T1
  B2 -.-> A2
  C1 -.-> F2
  E3 -.-> F1
  F1 -.-> P3
  F1 -.-> P4
  D -.-> P1
  A1 -.-> P2
```

## 讀圖要點

1. **兩個匯點**:存在線收於 G4(CDC);必然線沒有匯點——它的四個端點
   (T2、B2、C1、E3)各自「回望」存在線上的一個對象(虛線),宣告其必然性。
   這四條虛線就是論文的「intrinsic necessity」一章。
2. **dim-3 閘門唯一**:F1(`annihilator_unique`)。必然線全程任意 Γ——
   所以「為什麼是 F₂³」的答案在存在線上(計數表示需要 codim 1),
   而不在結構本身。
3. **選擇隔離點**(全部已證無選擇性):κ(`kappa_eq`)、`next` 伴 dart
   (`cfam_eq_kappa`)、E3 的補空間/分離泛函(只進證明不進陳述)、
   Rank 橋的 `decide`(隔離葉檔)。
4. **橋的地位**:SLOT 不在主幹上——刪掉它,兩條線的數學完好無損;
   但「本文是對原證明的等價重構」這一**關於兩個文本之關係**的主張,
   其形式承載就是它。方法論意義上必要,數學意義上外掛。
5. **G(CDC 抽取)設計**:dart 語言的增益延續到底——「圈」不必以頂點路徑
   形式化:在 M_s 的 dart 集上,`ρ := 頂點配對 ∘ σ` 是置換,
   **圈 = ρ-軌道**,分解定理 = 置換軌道分解。G1 由 T1 的覆蓋計數逐頂點
   拉回;G3 由 B1(線 = 二點集)即得。G4 是條件形式
   (三次 + 給定 NWZ 流;8-flow 存在性與三次化約引文獻,不入庫)。
